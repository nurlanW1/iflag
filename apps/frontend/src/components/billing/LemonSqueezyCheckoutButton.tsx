/**
 * @deprecated Use `CheckoutButton` from './CheckoutButton'. Kept as a thin
 * alias so existing imports keep working while the codebase migrates to the
 * provider-agnostic name.
 */

export { CheckoutButton as LemonSqueezyCheckoutButton } from './CheckoutButton';
export type { CheckoutKind } from './CheckoutButton';
